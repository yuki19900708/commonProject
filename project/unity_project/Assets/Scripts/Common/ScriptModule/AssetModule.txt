using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;

public class #ScriptName#Table : ScriptableObject
{
    public List<#DataName#> #ListName#Table = new List<#DataName#>();
    public Dictionary<int, #DataName#> #ListName#Dic = new Dictionary<int, #DataName#>();

    public void SetDatas(object[] obj)
    {
        #ListName#Table.Clear();
        foreach (object o in obj)
        {
            #ListName#Table.Add(o as #DataName#);
        }
    }

    public List<#DataName#> GetAllData()
    {
        if (#ListName#Table == null || #ListName#Table.Count == 0)
        {
            Debug.LogError("#ScriptName#Table未导入asset");
        }
        return #ListName#Table;
    }

    public IList<#DataName#> GetAllReadonlyData()
    {
        if (#ListName#Table == null || #ListName#Table.Count == 0)
        {
            Debug.LogError("#ScriptName#Table未导入asset");
        }

        ReadOnlyCollection<#DataName#> readOnly#DataName# = new ReadOnlyCollection<#DataName#>(#ListName#Table);

        return readOnly#DataName# ;
    }

    public #DataName# GetData(int index)
    {
#Content#
    }
}

