        if (#ListName#Table == null || #ListName#Table.Count == 0)
        {
            Debug.LogError("#ScriptName#Table未导入asset");
        }
        if (#ListName#Dic.Count == 0)
        {
            ReadOnlyCollection<#DataName#> readOnly#DataName# = new ReadOnlyCollection<#DataName#>(#ListName#Table);
            foreach (#DataName# value in readOnly#DataName#)
            {
                if (#ListName#Dic.ContainsKey(value.id))
                {
                    Debug.LogError("id重复检查数据表"+ value.id);
                }
                //#ListName#Dic.Add(value.id, value.Clone());
                #ListName#Dic.Add(value.id, value);
            }
        }
        if (#ListName#Dic.ContainsKey(index))
        {
            return #ListName#Dic[index];
        }
        else
        {
            return null;
        }
        